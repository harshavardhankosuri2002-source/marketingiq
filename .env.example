# MarketingIQ Environment Variables Configuration
# Placeholders only - Never commit real secrets or API keys.

# Optional Server-side LLM Integration (When unset, MarketingIQ runs in deterministic Analytics Mode)
LLM_API_KEY=your_llm_api_key_here
LLM_MODEL=gemini-1.5-pro

# Environment Indicator
NODE_ENV=production
